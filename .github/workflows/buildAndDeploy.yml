name: Push Trigger Workflow

on:
  push:
    branches: 
      - '*'
  workflow_call:

jobs:
  run-on-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
      - uses: actions/checkout@v4

      - name: Demo job
      - uses: echo "This workflow was triggered by a push"
  